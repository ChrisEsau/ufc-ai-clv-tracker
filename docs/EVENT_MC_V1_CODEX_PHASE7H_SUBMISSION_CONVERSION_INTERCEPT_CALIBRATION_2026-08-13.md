# EVENT MC V1 — Phase 7H Global Submission Conversion Intercept Calibration

Phase 7G diagnostic execution is complete with no promotion: `submission_attempts.base_30s` remains 0.045. Calibrate only `defaults.submission_finish.intercept` end to end against historical SUB fight share on chronological train and holdout cohorts.

Attempt base 0.045, bottom multiplier 1.0, both position bonuses 0.0, KD midpoint 36, finish midpoint 36, and every other mechanic/config value are frozen. Use common seeds, report split-specific method errors and all attempt, conversion, ground, timing, round, KD and KO guardrails. Promote only when both temporal splits support the same stable region.

Expected final line: `PHASE 7H GLOBAL SUBMISSION CONVERSION INTERCEPT CALIBRATION GATE: PASS` or FAIL.
