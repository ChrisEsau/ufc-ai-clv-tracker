name: KO V3 From Scratch Stage 1 Ablation

on:
  push:
    branches:
      - research/ko-system-v3-from-scratch-20260826
    paths:
      - 'pipeline/research/ko_v3_from_scratch_stage1.py'
      - 'pipeline/research/ko_v3_from_scratch_stage1_ablation.py'
      - '.github/workflows/ko-v3-from-scratch-stage1-ablation.yml'
  workflow_dispatch:

jobs:
  ko-v3-stage1-ablation:
    runs-on: ubuntu-24.04
    steps:
      - name: Sparse shallow checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          sparse-checkout: |
            pipeline
            data/master
            data/fight_details
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install dependencies
        run: python -m pip install numpy pandas pyarrow scikit-learn
      - name: Run KO V3 Stage 1b ablation
        run: |
          set -euo pipefail
          mkdir -p artifacts/ko-v3-from-scratch-stage1-ablation
          python -m pipeline.research.ko_v3_from_scratch_stage1_ablation \
            --first-test-year 2020 \
            --out-dir artifacts/ko-v3-from-scratch-stage1-ablation \
            | tee artifacts/ko-v3-from-scratch-stage1-ablation/result.txt
      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: ko-v3-from-scratch-stage1-ablation-${{ github.run_id }}
          path: artifacts/ko-v3-from-scratch-stage1-ablation
          if-no-files-found: error
          retention-days: 30
