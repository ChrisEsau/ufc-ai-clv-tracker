name: LOCKED Brain MC Allen Shahbazyan

on:
  push:
    branches:
      - research/ko-system-v3-from-scratch-20260826
    paths:
      - pipeline/research/locked_brain_mc_allen_shahbazyan.py
      - .github/workflows/locked-brain-mc-allen-shahbazyan.yml
  workflow_dispatch:

permissions:
  contents: read
  actions: read

jobs:
  run:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Install locked runner dependencies
        run: python -m pip install numpy pandas pyarrow scipy scikit-learn pyyaml joblib
      - name: Restore canonical FSR V3 artifact
        uses: actions/download-artifact@v4
        with:
          artifact-ids: 9494902022
          path: restored
          github-token: ${{ secrets.GITHUB_TOKEN }}
          repository: ChrisEsau/ufc-ai-clv-tracker
          run-id: 32645607979
      - name: Run ONLY locked Brain MC harness
        run: |
          set -euo pipefail
          cp -a restored/canonical-weight-class-state/. data/
          python -m pipeline.research.locked_brain_mc_allen_shahbazyan | tee locked-brain-mc-allen-shahbazyan.txt
      - name: Upload locked outputs
        uses: actions/upload-artifact@v4
        with:
          name: locked-brain-mc-allen-shahbazyan-${{ github.run_id }}
          path: |
            locked-brain-mc-allen-shahbazyan.txt
            data/research/locked_brain_mc_allen_shahbazyan/
