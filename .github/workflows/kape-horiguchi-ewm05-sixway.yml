name: Kape Horiguchi EWM05 Six-Way Shadow

on:
  push:
    branches:
      - research/kape-horiguchi-ewm05-sixway-20260826

permissions:
  contents: read
  actions: read

jobs:
  kape-horiguchi-ewm05-sixway:
    runs-on: ubuntu-24.04
    steps:
      - name: Sparse shallow checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
          sparse-checkout: |
            pipeline
            configs
            config
            data/master
            data/fight_details

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: python -m pip install numpy pandas pyarrow pyyaml scipy scikit-learn joblib

      - name: Restore canonical historical Brain MC inputs
        uses: actions/download-artifact@v4
        with:
          artifact-ids: 9494902022
          path: restored
          github-token: ${{ github.token }}
          repository: ChrisEsau/ufc-ai-clv-tracker
          run-id: 32645607979

      - name: Run Kape Horiguchi EWM05 six-way only
        shell: bash
        run: |
          set -o pipefail
          cp -a restored/canonical-weight-class-state/. data/
          mkdir -p artifacts/kape-horiguchi-ewm05-sixway
          python -m pipeline.simulation.event_clock_mc_v2.diagnostics.kape_horiguchi_ewm05_sixway \
            | tee artifacts/kape-horiguchi-ewm05-sixway/result.txt

      - name: Upload result
        uses: actions/upload-artifact@v4
        with:
          name: kape-horiguchi-ewm05-sixway-${{ github.run_id }}
          path: artifacts/kape-horiguchi-ewm05-sixway/result.txt
          if-no-files-found: error
          retention-days: 30
