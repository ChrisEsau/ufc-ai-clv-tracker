name: Run Master Column Validation

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run-master-column-validation:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Pull latest repo state
        run: |
          git pull --rebase origin main

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          pip install pandas numpy pyarrow

      - name: Run master column validation
        run: |
          python run_master_column_validation.py

      - name: Commit validation output
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"

          git add ufc_master_column_validation.parquet

          git commit -m "Update master column validation artifact" || echo "No changes to commit"

          git push
