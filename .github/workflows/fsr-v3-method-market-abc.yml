name: FSR V3 Legacy Method Market ABC Validation

# Retired 2026-08-22: canonical Event Clock C is now the standard model.
# This workflow remains manually dispatchable only for historical comparison.
# Normal FSR V3 development must use the canonical-C validation workflow and
# must not spend paths re-running obsolete A/B arms.
on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy comparison notice
        run: |
          echo "A/B/C automatic validation is retired."
          echo "Canonical C is the standard model."
          echo "Use FSR V3 Canonical C Validation for current guardrails."
