name: Event Clock V2 FSR Residual Feature Screen

on:
  workflow_dispatch:
  push:
    branches:
      - research/weight-class-audit-20260823
    paths:
      - ".github/workflows/fsr-v3-canonical-c.yml"
      - "pipeline/simulation/event_clock_mc_v2/diagnostics/fsr_residual_feature_screen.py"

permissions:
  contents: read
  actions: read

jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      PYTHONPATH: .
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install focused dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install numpy pandas pyarrow scikit-learn

      - name: Download frozen canonical research state
        uses: actions/download-artifact@v4
        with:
          name: canonical-weight-class-state
          path: data
          github-token: ${{ secrets.GITHUB_TOKEN }}
          repository: ChrisEsau/ufc-ai-clv-tracker
          run-id: 32645607979

      - name: Verify feature-screen inputs
        run: |
          test -f data/master/ufc_master.parquet
          test -f data/market/historical_market_outcomes.parquet
          test -f data/fsr_v3/fsr_v3_prefight_snapshots.parquet
          python -m py_compile pipeline/simulation/event_clock_mc_v2/diagnostics/fsr_residual_feature_screen.py

      - name: Run chronological residual feature-family screen
        run: |
          set -o pipefail
          python -m pipeline.simulation.event_clock_mc_v2.diagnostics.fsr_residual_feature_screen \
            --out-dir data/diagnostics/event_clock_mc_v2/fsr_residual_feature_screen \
            2>&1 | tee /tmp/fsr_residual_feature_screen.log

      - name: Print feature screen
        if: always()
        run: |
          if [ -f /tmp/fsr_residual_feature_screen.log ]; then
            cat /tmp/fsr_residual_feature_screen.log
          fi

      - name: Upload feature-screen outputs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: event-clock-v2-fsr-residual-feature-screen
          path: data/diagnostics/event_clock_mc_v2/fsr_residual_feature_screen/*.csv
          if-no-files-found: warn
